# 🎀 MeoXP Discord Bot

บอท Discord สำหรับเล่นเพลงและควบคุมระบบ พร้อม UI ปุ่มกดแบบ Interactive

![Discord](https://img.shields.io/badge/Discord-Bot-7289DA?style=for-the-badge&logo=discord&logoColor=white)
![Python](https://img.shields.io/badge/Python-3.11+-3776AB?style=for-the-badge&logo=python&logoColor=white)
![Docker](https://img.shields.io/badge/Docker-Ready-2496ED?style=for-the-badge&logo=docker&logoColor=white)

---

## ✨ Features

### 🎵 ระบบเพลง

- เล่นเพลงจาก YouTube และแหล่งอื่นๆ
- ระบบ Queue รองรับหลายเพลง
- ปุ่มควบคุมแบบ Interactive (Pause, Skip, Queue, Stop)
- คุณภาพเสียงสูง 320kbps

### 💬 ส่งข้อความ

- ส่งข้อความไปยัง User หรือ Channel อื่น
- ดึงไฟล์จาก Server

### ⚙️ ระบบ (Owner Only)

- รันคำสั่ง Shell บน Server

---

## 🚀 วิธีติดตั้ง & รัน Docker

### ขั้นตอนที่ 1: เตรียม Environment

ตรวจสอบว่ามี Docker และ Docker Compose ติดตั้งแล้ว:

```bash
# เช็ค Docker
docker --version

# เช็ค Docker Compose
docker compose version
```

> 💡 ถ้ายังไม่มี Docker ให้ติดตั้งก่อน:
>
> - **Debian/Ubuntu**: `sudo apt update && sudo apt install docker.io docker-compose-plugin`
> - **macOS**: ติดตั้ง [Docker Desktop](https://www.docker.com/products/docker-desktop/)

---

### ขั้นตอนที่ 2: Clone Repository (ถ้ายังไม่มี)

```bash
git clone https://github.com/VacTuzX-dot/meoxp-bot.git
cd meoxp-bot
```

---

### ขั้นตอนที่ 3: สร้างไฟล์ .env

สร้างไฟล์ `.env` ในโฟลเดอร์โปรเจกต์:

```bash
nano .env
```

เพิ่ม Token ของบอท Discord:

```env
DISCORD_TOKEN=ใส่_TOKEN_ของคุณ_ที่นี่
```

> 🔐 **วิธีหา Token**:
>
> 1. ไปที่ [Discord Developer Portal](https://discord.com/developers/applications)
> 2. เลือก Application ของคุณ
> 3. ไปที่ **Bot** → กด **Reset Token** → Copy Token

บันทึกไฟล์: `Ctrl+O` → `Enter` → `Ctrl+X`

---

### ขั้นตอนที่ 4: Build Docker Image

```bash
docker compose build
```

⏳ รอสักครู่... Docker จะ:

1. ดาวน์โหลด Python 3.11 image
2. ติดตั้ง ffmpeg (สำหรับเล่นเพลง)
3. ติดตั้ง Python dependencies

---

### ขั้นตอนที่ 5: รัน Bot

```bash
docker compose up -d
```

> 📝 อธิบาย:
>
> - `docker compose up` = สร้างและรัน container
> - `-d` = รันใน background (detached mode)

---

### ขั้นตอนที่ 6: ตรวจสอบสถานะ

```bash
# ดู logs ของ bot
docker compose logs -f

# ดู container ที่รันอยู่
docker compose ps
```

ถ้าเห็นข้อความ:

```
Logged in as MeoXP Bot#1234 (ID: 123456789)
------ System Online ------
```

✅ **บอทพร้อมใช้งานแล้ว!**

---

## 🔧 คำสั่งจัดการ Docker

| คำสั่ง                            | อธิบาย               |
| --------------------------------- | -------------------- |
| `docker compose up -d`            | รัน bot (background) |
| `docker compose down`             | หยุด bot             |
| `docker compose restart`          | รีสตาร์ท bot         |
| `docker compose logs -f`          | ดู logs แบบ realtime |
| `docker compose build --no-cache` | Build ใหม่ทั้งหมด    |

---

## 📱 คำสั่งบอท

### 🎵 เพลง

| คำสั่ง                   | อธิบาย                      |
| ------------------------ | --------------------------- |
| `!play <ลิงก์/ชื่อเพลง>` | เล่นเพลงหรือเพิ่มเข้า Queue |
| `!pause`                 | หยุดเพลงชั่วคราว            |
| `!resume`                | เล่นเพลงต่อ                 |
| `!skip`                  | ข้ามไปเพลงถัดไป             |
| `!queue` หรือ `!q`       | ดูรายการเพลงใน Queue        |
| `!np`                    | ดูเพลงที่กำลังเล่น          |
| `!clear`                 | ล้าง Queue                  |
| `!stop`                  | หยุดเพลงและออกจากห้อง       |

### 💬 ข้อความ

| คำสั่ง                                 | อธิบาย                  |
| -------------------------------------- | ----------------------- |
| `!sendtext <@user/#channel> <ข้อความ>` | ส่งข้อความ (Owner only) |
| `!getfile <ชื่อไฟล์>`                  | ดึงไฟล์จาก Server       |

### ⚙️ ระบบ

| คำสั่ง          | อธิบาย                       |
| --------------- | ---------------------------- |
| `!cmd <คำสั่ง>` | รันคำสั่ง Shell (Owner only) |
| `!help`         | แสดงเมนูช่วยเหลือ            |

---

## 🔄 อัพเดทบอท

เมื่อมีการอัพเดทโค้ดใหม่:

```bash
cd meoxp-bot
git pull
docker compose down
docker compose build --no-cache
docker compose up -d
```

---

## 🐛 Troubleshooting

### บอทไม่ออนไลน์

```bash
# ดู logs
docker compose logs

# ตรวจสอบว่า container รันอยู่
docker compose ps
```

### เพลงไม่เล่น

- ตรวจสอบว่า ffmpeg ติดตั้งใน container แล้ว
- ลอง rebuild: `docker compose build --no-cache`

### Token ไม่ถูกต้อง

- ตรวจสอบไฟล์ `.env`
- Token ต้องไม่มีช่องว่างหรือ quote

---

## 📁 โครงสร้างโปรเจกต์

```
meoxp-bot/
├── main.py              # โค้ดหลักของบอท
├── Dockerfile           # Docker image configuration
├── docker-compose.yml   # Docker Compose configuration
├── requirements.txt     # Python dependencies
├── .env                 # Discord Token (ไม่อยู่ใน Git)
├── .gitignore          # ไฟล์ที่ไม่ track ใน Git
├── README.md           # เอกสารนี้
└── LICENSE             # ลิขสิทธิ์
```

---

## 📄 License

© 2024 VacTuzX. All Rights Reserved.

โปรเจกต์นี้สงวนลิขสิทธิ์ทั้งหมด ห้ามคัดลอก แก้ไข หรือเผยแพร่โดยไม่ได้รับอนุญาต

ดูรายละเอียดเพิ่มเติมที่ [LICENSE](./LICENSE)

---

## 👨‍💻 ผู้พัฒนา

**VacTuzX**

- GitHub: [@VacTuzX-dot](https://github.com/VacTuzX-dot)

---

<p align="center">Made with 💕 by VacTuzX</p>
